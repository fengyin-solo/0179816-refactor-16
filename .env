# 唯一配置口径：本地开发（vite.config.js）与容器（docker-compose.yml / Dockerfile）共用
# 修改端口只需改这一处，两边同时生效
PORT=8081

# 构建阶段使用的 Node 版本（镜像 tag 后缀，对应 node:${NODE_VERSION}-alpine）
# package.json 的 engines.node 与之保持一致，用于约束本地开发环境
NODE_VERSION=20
